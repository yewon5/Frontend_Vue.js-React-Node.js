import { createApp } from 'vue'
//import App from './exam1/App.vue'

/*
import App from './exam2/App.vue'

import ListFooter from './exam2/components/ListFooter.vue'
import ListHeader from './exam2/components/ListHeader.vue'
import ListPage from './exam2/components/ListPage.vue'

const app = createApp(App)
//"" 태그명을 지정해 줄 수 있는데 컴포넌트 태그 할 때 이미 케밥 방식으로 등록했기 때문에 그대로 사용한다.
app.component("list-header", ListHeader) 
app.component("list-footer", ListFooter)
app.component("list-page", ListPage)
app.mount('#app')
*/

//import App from './exam3/App.vue'

import App from './exam4/App.vue'

createApp(App).mount("#app");